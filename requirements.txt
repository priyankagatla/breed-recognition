# Web application (required to run app.py)
Flask==3.1.3
Pillow==11.1.0

# Only needed if you run download_real_images.py to fetch real breed photos
requests==2.32.3

# Training pipeline (required only to run train_model.py / prepare_dataset.py)
tensorflow==2.16.1
numpy==1.26.4
matplotlib==3.9.2
scikit-learn==1.5.2
